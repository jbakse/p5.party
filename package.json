{
  "name": "deepstream_test",
  "version": "0.0.1",
  "description": "First pass at making a easy to use library for simple multiuser sketches.",
  "main": "index.js",
  "scripts": {
    "start": "PORT=${PORT:-6020} deepstream start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin Bakse",
  "license": "UNLICENCED",
  "dependencies": {
    "@deepstream/client": "^5.0.8",
    "@deepstream/server": "^5.0.16"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.0.0",
    "eslint-config-p5js": "^1.0.1"
  },
  "engines": {
    "node": "14.1.0"
  }
}
